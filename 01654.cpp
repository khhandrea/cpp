// 01654번 랜선 자르기
#include <cstdio>

using namespace std;

int main() {
    int K, N, lan, i;
    int *lans;

    // Input
    scanf("%d %d", &K, &N);
    lans = new int[K];
    for (i=0; i<K; i++) {
        scanf("%d", &lan);
        lans[i] = lan;
    }

    // Solve

    // Output

    return 0;
}